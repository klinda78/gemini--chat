flowchart TD   
    subgraph Control_Flow["控制流"]
        CRON[Cron Trigger]
        AGENT[Agent]
        WORKFLOW["Workflow (Lobster)"]
        TOOLS[Tools / CLI / Skills / llm-task]
    end

    CRON --> AGENT --> WORKFLOW --> TOOLS

```mermaid

```mermaid
flowchart TD  
    subgraph Data_Flow["数据流"]
        SKILL_CLI["Skill CLI (e.g., openclaw skills run)"]
        LLM["llm-task (JSON output)"]
        JQ["jq -r '.markdown'"]
        MESSAGE["openclaw message send (to Feishu)"]
    end

    SKILL_CLI --> LLM --> JQ --> MESSAGE
```mermaid

```mermaid
flowchart TD  
    subgraph Slash_Command["Slash Command /skill ..."]
        SC["仅可在聊天界面触发\n不可写入 workflow command"]
    end
```mermaid
